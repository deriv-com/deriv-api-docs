{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Access Partner Toolkit Services (response)",
    "description": "Access services provided via Partner Toolkit for a specific partner.",
    "type": "object",
    "required": [
        "echo_req",
        "msg_type"
    ],
    "properties": {
        "partner_toolkit": {
            "title": "partner_toolkit",
            "description": "Partner Toolkit specific information and response",
            "type": "object",
            "additionalProperties": false,
            "properties": {
                "postly_redirect_url": {
                    "description": "Partner-specific redirect URL for postly.",
                    "type": "string"
                }
            }
        },
        "echo_req": {
            "description": "Echo of the request made.",
            "type": "object"
        },
        "msg_type": {
            "description": "Action name of the request made.",
            "type": "string",
            "enum": [
                "partner_toolkit"
            ]
        },
        "req_id": {
            "description": "Optional field sent in request to map to response, present only when request contains `req_id`.",
            "type": "integer"
        }
    }
}
